// clonar array

const notas = [7, 7, 8, 9];

// spread operator
const novaNotas = [...notas, 10]; // Ele vai pegar os valores do array notas e vai jogar na nova array e adicionar o valor após a vírgula, 10

console.log(notas);
console.log(novaNotas);